//what is between
function between(a, b) {
    // create an array to push values into
    let list =[]
    //create a loop that pushes the values
    for(let i = a; i <= b; i++){
      list.push(i)
    }
    return list
  }